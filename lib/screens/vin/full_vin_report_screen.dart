import 'package:flutter/material.dart';

class FullVinReportScreen extends StatelessWidget {
  const FullVinReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Full VIN Report Screen")),
    );
  }
}
